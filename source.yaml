canonical:
  # for source = Local
  # ==========
  # type: local
  # path: ./config.yaml

  # for source = Git
  # ==========
  type: git
  repo: https://github.com/neel-03/configdrift.git
  branch: test/remote_config
  path: config.yaml
  auth_token: $GITHUB_ACCESS_TOKEN

interval: 20s
